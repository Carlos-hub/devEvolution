<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>Informações do Usuário</title>
    <style>
      body {
        background-color: #282828;
        color: #ffffff;
        font-family: "Helvetica Neue", sans-serif;
      }
      h1 {
        font-size: 36px;
        text-align: center;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-top: 50px;
      }
      ul {
        list-style: none;
        margin: 0;
        padding: 0;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
      }
      li {
        font-size: 24px;
        margin: 20px;
        padding: 20px;
        background-color: #555555;
        border-radius: 10px;
        box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.5);
        text-align: center;
        min-width: 300px;
      }
      li strong {
        display: block;
        font-size: 18px;
        margin-bottom: 10px;
        color: #cccccc;
      }
    </style>
  </head>
  <body>
    <h1>Informações do Usuário</h1>
    <ul>
      <li>
        <strong>Nome:</strong> <%= user.nome %>
      </li>
      <li>
        <strong>Idade:</strong> <%= user.idade %>
      </li>
      <li>
        <strong>Endereços de Email:</strong>
        <ul>
          <% for (var i = 0; i < user.emails.length; i++) { %>
            <li><%= user.emails[i] %></li>
          <% } %>
        </ul>
      </li>
      <li>
        <strong>Senha:</strong> ********
      </li>
      <li>
        <strong>Data de Nascimento:</strong> <%= user.dataNascimento %>
      </li>
      <li>
        <strong>Histórico de Pedidos:</strong>
        <ul>
          <% for (var i = 0; i < user.pedidos.length; i++) { %>
            <li><%= user.pedidos[i] %></li>
          <% } %>
        </ul>
      </li>
    </ul>
  </body>
</html>
